import { map } from './map'

export memoize(priorObservable, n = 1) ->
  memory = []
  priorObservable~map((x) ->
    memory.unshift(x)
    observerWillSee = memory.slice()
    if memory.length > n: memory.pop()
    observerWillSee
  )
