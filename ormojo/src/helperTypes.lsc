import { entity, field, types } from '@ormojo/ephraim'

@entity({name: 'Timestamped'}) class Timestamped:
  @field(types.date) createdAt
  @field(types.date) updatedAt

@entity({name: 'Tombstoned'}) class Tombstoned:
  @field(types.date) deletedAt

export { Timestamped, Tombstoned }
