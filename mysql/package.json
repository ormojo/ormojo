{
  "name": "@ormojo/mysql",
  "version": "0.4.4",
  "license": "MIT",
  "description": "MySQL driver for ormojo.",
  "type": "commonjs",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "clean": "rimraf lib coverage .nyc_output",
    "build": "rollup -c",
    "build:coverage": "cross-env COVERAGE=true rollup -c",
    "test": "npm run clean && npm run build && jest",
    "test:only": "jest",
    "test:debug": "node --inspect-brk $(npm bin)/jest -i --env jest-environment-node-debug",
    "coverage": "npm run clean && npm run build:coverage && jest --coverage",
    "lint": "eslint --ext .js,.lsc src",
    "preversion": "npm run lint && npm run build"
  },
  "dependencies": {
    "@ormojo/ephraim": "^1.0.3",
    "@ormojo/ormojo": "^1.1.2",
    "lodash": "^4.17.21",
    "sqlstring": "^2.3.2"
  },
  "devDependencies": {
    "jest": "^27.0.4",
    "mysql2": "^2.2.5",
    "npm-run-all": "^4.1.5"
  },
  "jest": {
    "testEnvironment": "node",
    "moduleFileExtensions": [
      "js",
      "json",
      "lsc"
    ],
    "transform": {
      "\\.lsc$": [
        "babel-jest",
        {
          "rootMode": "upward"
        }
      ]
    },
    "testRegex": "(\\.|/)test\\.lsc$",
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/lib/"
    ]
  }
}
